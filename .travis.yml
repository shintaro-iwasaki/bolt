sudo: false
language: c

dist: bionic

os:
  - linux
  - osx

env:
  - USE_ARGOBOTS=TRUE
  - USE_ARGOBOTS=FALSE

script:
  - mkdir build && cd build
  - cmake ../ -DLIBOMP_USE_ARGOBOTS=${USE_ARGOBOTS} -DOPENMP_ENABLE_WERROR=TRUE
  - make -j 2
